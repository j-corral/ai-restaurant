meta {
  name: Get Order (Public)
  type: http
  seq: 2
}

get {
  url: {{apiUrl}}/api/orders/{{orderId}}
  body: none
  auth: none
}

tests {
  test("Order retrieved successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property('id');
    expect(res.getBody()).to.have.property('items');
  });
}
