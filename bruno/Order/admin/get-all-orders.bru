meta {
  name: Get All Orders (Admin)
  type: http
  seq: 1
}

get {
  url: {{apiUrl}}/api/orders/admin/all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("All orders retrieved successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}
