meta {
  name: Get Menu by Category (Public)
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/api/menu/category/pizza
  body: none
  auth: none
}

tests {
  test("Menu by category retrieved successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}
