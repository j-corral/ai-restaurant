meta {
  name: Get Menu Item (Admin)
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/menu/admin/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Menu item retrieved successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property('id');
  });
}
