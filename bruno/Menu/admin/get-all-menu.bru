meta {
  name: Get All Menu Items (Admin)
  type: http
  seq: 1
}

get {
  url: {{apiUrl}}/api/menu/admin/all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("All menu items retrieved successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}
