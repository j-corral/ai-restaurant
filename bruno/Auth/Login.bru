meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "massimo",
    "password": "montolli"
  }
}

settings {
  encodeUrl: true
}
